--- # Connection
- name: Install packages
  hosts: localhost
  become: true
  become_method: sudo 
  user: ec2-user
  tasks: 





  # Task block
  - name: Install telnet 
    ansible.builtin.package:
      name: telnet
      state: absent            #present       #latest